(module processing/constants racket

  (provide (all-defined-out))

  (define HALF_PI (/ pi 2))
  (define PI pi)
  (define QUARTER_PI (/ pi 4))
  (define TAU (* pi 2))
  (define TWO_PI (* pi 2))

  )
